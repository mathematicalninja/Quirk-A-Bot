/**
 * Used for Quick-Testing new code.
 * @param {Object} msg Discord message object
 */
export default function TestTextOutput(msg) {
	// let replyString = "";
	// console.log(msg);
	// console.log(replyString);
	// msg.reply(replyString);
};